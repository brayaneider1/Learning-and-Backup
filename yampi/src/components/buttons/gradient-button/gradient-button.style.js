import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  button: {
    marginHorizontal: 0,
  },
  gradient: {
    height: "100%",
    width: "100%",
    alignItems: "center",
    justifyContent: "center"
  }
});